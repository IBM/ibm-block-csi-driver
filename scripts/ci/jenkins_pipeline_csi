pipeline {
    agent {
        label 'docker-engine'
    }
    stages {
        stage ('Run CSI controller unittests (inside container)') {
            steps {
                sh './scripts/run_unitests.sh'
            }
        }
        stage ('Build CSI controller image') {
            steps {
                sh './scripts/ci/build_push_images.sh'
            }
        }
	}
}
