pipeline {
    agent {
        label 'ansible_rhel73'
    }
    stages {
        }
        stage ('Run unittests') {
            steps {
                sh 'nosetests --cover-package=controller --with-coverage'
            }
        }
	}
}
